import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.web';

// ReactDOM.createRoot를 사용하여 루트를 생성하고 렌더링합니다.
const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(<App />);
